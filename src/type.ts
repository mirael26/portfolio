export interface IEducationInfo {
  year: string;
  title: string;
  institution: string;
}

export interface IExperienceInfo {
  period: string;
  company: string;
  position: string; 
  description: string;
}
